## vim emulator for vscode setting

setting.json

* ```<C-j> is <Esc>```
* ```* is highlight```
* ```<C-/> is comment out``` 
* ```<C-k>,<C-f> is format all document```
* ```<C-n> is next suggestion, <C-p> is prev suggestion```
